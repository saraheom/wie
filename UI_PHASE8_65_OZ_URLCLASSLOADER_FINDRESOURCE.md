# UI Phase 8.65 — OZ URLClassLoader.findResource localization

Diagnostic-only phase. No UI behavior changes. Adds targeted runtime markers around `URLClassLoader.findResource()` and `URL.getFile()` for the OZ LGT startup black screen.
